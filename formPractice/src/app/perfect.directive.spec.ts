import { PerfectDirective } from './perfect.directive';

describe('PerfectDirective', () => {
  it('should create an instance', () => {
    const directive = new PerfectDirective();
    expect(directive).toBeTruthy();
  });
});
